# Pipeline de ingestão — dependências mínimas de propósito.
# Sem pandas: os arquivos de fonte pública vão de 2 MB (Conab) a 1,3 GB
# (PGFN não previdenciário). Streaming com csv/zipfile da stdlib aguenta
# qualquer um deles em memória constante; pandas não.
httpx==0.27.2
neo4j==5.24.0
python-dotenv==1.0.1
